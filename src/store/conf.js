

export class Conf 
{
    constructor(opts) 
    {
        // proxy local configuration object
        //  on change, save to file

        // keep stream open to config file?
        
    }
    load() {
        // read config file to memory
    }
}